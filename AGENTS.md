# Knowledge Base

This repository uses a decentralized knowledge base under `kb/` directories across the tree.

When navigating or modifying this codebase, always start by reading the root `kb/AGENTS.md` index file and then any local `kb/AGENTS.md` files for the directories you touch. The root index defines the KB conventions, required headers, and when KB files must be updated.
